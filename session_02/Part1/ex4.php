<?php
  // 1. Create the boolean variable
  $isOnline = true;

  // 2. Use Ternary Operator to print status
  // Syntax: (Condition) ? (Value if True) : (Value if False)
  echo $isOnline ? "User is Online" : "User is Offline";
?>